speechrecognition==3.10.0
pyttsx3==2.90
pyaudio==0.2.13

⚠️ Note: pyaudio may require system-level installation on some machines:

For Windows: Use pip install pipwin and then pipwin install pyaudio

For Linux: Run sudo apt install portaudio19-dev before installing
